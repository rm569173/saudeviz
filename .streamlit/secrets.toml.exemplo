# Modelo de configuracao de credenciais.
#
# Copie para .streamlit/secrets.toml no ambiente local, ou cole o conteudo em
# Settings > Secrets no Streamlit Community Cloud.
#
# ATENCAO: secrets.toml esta no .gitignore e NUNCA deve ser versionado.

[oracle]
user = "rm569173"
password = "SUA_SENHA_AQUI"
dsn = "(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=oracle.fiap.com.br)(PORT=1521))(CONNECT_DATA=(SID=orcl)))"
